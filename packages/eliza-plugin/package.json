{
  "name": "@agentos/eliza-plugin",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "description": "AgentOS memory provider for ElizaOS — persistent, versioned, searchable agent memory",
  "keywords": ["agentos", "elizaos", "ai16z", "eliza", "agent", "memory", "solana"],
  "license": "MIT",
  "scripts": {
    "dev": "tsc -p tsconfig.json --watch",
    "build": "tsc -p tsconfig.json",
    "lint": "eslint . --max-warnings=0",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@agentos/sdk": "workspace:*"
  },
  "peerDependencies": {
    "@elizaos/core": ">=0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "eslint": "^9.14.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.8"
  }
}
