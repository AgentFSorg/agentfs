services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_USER: postgres
      POSTGRES_DB: agentos
    ports:
      - "5432:5432"
    volumes:
      - agentos_db:/var/lib/postgresql/data
volumes:
  agentos_db:
